(define 0 (quote zero))

0

,p

(define list
  (lambda x x))

,p

(define 0 (quote (list second zero)))

,p

(define 1 (quote a))

(define 0 (list (quote second) (quote zero)))

,p

(define 1 0)

0
1
,p
